## 1. Dashboard Layout

- [x] 1.1 Apply responsive classes to StatCard container in `src/ui/src/pages/Dashboard.tsx` (hide on mobile)

## 2. Visualizer Improvements

- [x] 2.1 Update `zIndexRange` in `src/ui/src/components/dashboard/visualizer/AgentNode.tsx`
- [x] 2.2 Update `zIndexRange` in `src/ui/src/components/dashboard/visualizer/OracleNode.tsx`

## 3. Verification

- [x] 3.1 Verify dashboard StatCards are hidden on viewports < 1024px
- [x] 3.2 Verify dashboard StatCards are visible on viewports >= 1024px
- [x] 3.3 Verify visualizer labels stay behind the mobile sidebar when it is opened
- [x] 3.4 Build the UI to ensure no compilation errors: `npm run build:ui`
